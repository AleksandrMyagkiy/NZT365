name: Apply NZT v12 workout polish

on:
  workflow_dispatch:
  push:
    branches: [ nzt-v12 ]
    paths:
      - 'tools/apply_v11_reader.py'
      - '.github/workflows/apply_v11_reader.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: nzt-v12
      - name: Apply workout polish
        run: python tools/apply_v11_reader.py
      - name: Commit integration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/nzt365/app/V7Workout.kt
          if ! git diff --cached --quiet; then
            git commit -m "Polish NZT v12 workout experience"
            git push
          fi
